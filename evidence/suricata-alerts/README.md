# Suricata Evidence Status

Raw Suricata `fast.log` and `eve.json` exports are **pending and not included in this repository**. Alert names in `../../incident-report.md` were transcribed from the lab notes/screenshots and should be treated as leads until the raw records are exported and verified.

A future evidence package should preserve rule/version information, complete JSON records, flow IDs, source/destination fields, timestamps with timezone/UTC context, and cryptographic hashes of the originals.
